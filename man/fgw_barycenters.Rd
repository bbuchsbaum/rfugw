% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pot_api.R
\name{fgw_barycenters}
\alias{fgw_barycenters}
\title{Fixed-Support FGW Barycenters}
\usage{
fgw_barycenters(
  N,
  Ys,
  Cs,
  ps = NULL,
  lambdas = NULL,
  alpha = 0.5,
  fixed_structure = FALSE,
  fixed_features = FALSE,
  p = NULL,
  loss_fun = "square_loss",
  epsilon = 0.05,
  symmetric = TRUE,
  max_iter = 100L,
  tol = 1e-09,
  warmstartT = TRUE,
  solver = c("PGD", "PPA"),
  sinkhorn_max_iter = 500L,
  sinkhorn_tol = 1e-09,
  sinkhorn_method = c("scaling", "log"),
  precision = c("mixed", "double"),
  check_every = 10L,
  init_C = NULL,
  init_X = NULL,
  random_state = 1L,
  feature_cost_metric = c("euclidean", "sqeuclidean"),
  feature_cost_normalize = TRUE,
  barycenter_eps = 1e-12,
  verbose = FALSE
)
}
\arguments{
\item{N}{Number of barycenter nodes.}

\item{Ys}{List of feature matrices, one per sample (\verb{ns x d}).}

\item{Cs}{List of structure matrices, one per sample (\verb{ns x ns}).}

\item{ps}{Optional list of sample weights for each sample.}

\item{lambdas}{Optional sample weights across sets (default uniform).}

\item{alpha}{FGW feature/structure trade-off.}

\item{fixed_structure}{If \code{TRUE}, keep barycenter structure fixed at \code{init_C}.}

\item{fixed_features}{If \code{TRUE}, keep barycenter features fixed at \code{init_X}.}

\item{p}{Optional barycenter weights (\code{N}) (default uniform).}

\item{loss_fun}{Inner loss, currently only \code{"square_loss"} is supported.}

\item{epsilon}{Entropic regularization used for inner FGW solves.}

\item{symmetric}{Assume symmetric structure matrices in inner FGW solves.}

\item{max_iter}{Maximum outer barycenter iterations.}

\item{tol}{Stopping tolerance on barycenter updates.}

\item{warmstartT}{If \code{TRUE}, warm-start inner FGW with previous couplings.}

\item{solver}{Inner FGW solver (\code{"PGD"} or \code{"PPA"}).}

\item{sinkhorn_max_iter}{Inner Sinkhorn iterations for FGW solves.}

\item{sinkhorn_tol}{Inner Sinkhorn tolerance for FGW solves.}

\item{sinkhorn_method}{Sinkhorn variant (\code{"scaling"} or \code{"log"}).}

\item{precision}{Numeric precision mode for inner FGW solves.}

\item{check_every}{Outer FGW check interval for inner solves.}

\item{init_C}{Optional initial barycenter structure (\verb{N x N}).}

\item{init_X}{Optional initial barycenter feature matrix (\verb{N x d}).}

\item{random_state}{Optional seed used only when \code{init_C} is \code{NULL}.}

\item{feature_cost_metric}{Feature cost metric (\code{"euclidean"} or
\code{"sqeuclidean"}).}

\item{feature_cost_normalize}{If \code{TRUE}, normalize feature costs to \verb{[0, 1]}.}

\item{barycenter_eps}{Stabilizer for divisions in barycenter updates.}

\item{verbose}{If \code{TRUE}, print outer-iteration diagnostics.}
}
\value{
A list with \code{X}, \code{C}, \code{p}, \code{couplings}, \code{history}, \code{iterations},
and \code{objective}.
}
\description{
Learns a fixed-size barycenter support by alternating between (entropic) FGW
couplings and closed-form barycenter updates of structure and features.
}
