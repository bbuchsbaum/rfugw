% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linear_ot.R
\name{ot_emd}
\alias{ot_emd}
\title{Exact balanced linear transport}
\usage{
ot_emd(M, p = NULL, q = NULL, max_iter = 20000L, tol = 1e-12)
}
\arguments{
\item{M}{Cost matrix (\verb{ns x nt}).}

\item{p}{Source weights (default uniform). Renormalized to sum 1.}

\item{q}{Target weights (default uniform). Renormalized to sum 1.}

\item{max_iter}{Maximum simplex iterations.}

\item{tol}{Optimality tolerance.}
}
\value{
An \code{rfugw_result} with \code{plan}, \code{ot_dist}, \code{status}, and residuals.
}
\description{
Network-simplex / assignment backend. Reported \code{ot_dist} is \verb{<M, plan>}.
}
\examples{
M <- matrix(c(0, 2, 2, 0), 2, 2)
out <- ot_emd(M)
out$converged
ot_validate_plan(out, c(0.5, 0.5), c(0.5, 0.5))
}
