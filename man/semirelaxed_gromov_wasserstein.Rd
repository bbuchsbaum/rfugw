% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pot_api_ports.R
\name{semirelaxed_gromov_wasserstein}
\alias{semirelaxed_gromov_wasserstein}
\title{Semi-Relaxed Gromov-Wasserstein (non-entropic, square loss)}
\usage{
semirelaxed_gromov_wasserstein(
  C1,
  C2,
  p = NULL,
  loss_fun = "square_loss",
  symmetric = NULL,
  log = FALSE,
  G0 = NULL,
  max_iter = 10000L,
  tol_rel = 1e-09,
  tol_abs = 1e-09,
  random_state = 0,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{C1}{Source structure cost matrix (\verb{ns x ns}).}

\item{C2}{Target structure cost matrix (\verb{nt x nt}).}

\item{p}{Source weights (default uniform).}

\item{loss_fun}{Inner loss, currently only \code{"square_loss"} is supported.}

\item{symmetric}{If \code{NULL}, inferred from \code{C1} and \code{C2}; otherwise logical.}

\item{G0}{Optional initial semirelaxed plan (\verb{ns x nt}) satisfying
\code{rowSums(G0) == p}.}

\item{max_iter}{Max mirror-descent iterations.}

\item{tol_rel}{Relative stopping tolerance.}

\item{tol_abs}{Absolute stopping tolerance.}

\item{verbose}{If \code{TRUE}, print iterative error diagnostics.}
}
\value{
A list with \code{plan}, \code{q}, \code{srgw_dist}, \code{iterations}, \code{error}, and
\code{abs_error}.
}
\description{
POT-compatible non-entropic semirelaxed GW solver via conditional gradient.
}
