% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fgw.R
\name{fugw_kl}
\alias{fugw_kl}
\title{Fused Unbalanced Gromov-Wasserstein (KL divergence, Sinkhorn inner solver)}
\usage{
fugw_kl(
  Cx,
  Cy,
  wx = NULL,
  wy = NULL,
  reg_marginals = c(10, 10),
  epsilon = 0.01,
  alpha = 0.5,
  M = NULL,
  init_pi = NULL,
  max_iter = 100L,
  tol = 1e-07,
  max_iter_ot = 500L,
  tol_ot = 1e-07,
  rescale_plan = TRUE,
  check_every = 1L,
  precision = c("double", "mixed", "strict_double"),
  feature_weight = NULL,
  structure_weight = NULL
)
}
\arguments{
\item{Cx}{Source structure matrix.}

\item{Cy}{Target structure matrix.}

\item{wx}{Source weights (default uniform). Renormalized to sum 1.}

\item{wy}{Target weights (default uniform). Renormalized to sum 1.}

\item{reg_marginals}{Length-1 or length-2 marginal relaxation parameters.}

\item{epsilon}{Joint KL regularization parameter (entropic term).}

\item{alpha}{Legacy feature-cost coefficient, in \verb{[0, 1]}; the structure
coefficient is one. This convention differs from \code{fgw_entropic()}.}

\item{M}{Linear sample cost matrix (default \code{NULL}).}

\item{init_pi}{Optional initial sample coupling (\verb{nx x ny}).}

\item{max_iter}{Max BCD iterations.}

\item{tol}{BCD stopping tolerance (\code{L1} delta on sample coupling).}

\item{max_iter_ot}{Max unbalanced Sinkhorn iterations per inner solve.}

\item{tol_ot}{Inner Sinkhorn tolerance.}

\item{rescale_plan}{Whether to rescale successive plans to equal mass.}

\item{check_every}{Evaluate BCD stopping criterion every \code{check_every} iterations.}

\item{precision}{Numeric precision mode for the C++ solver (\code{"double"},
\code{"mixed"}, or \code{"strict_double"}). Tight mixed requests are promoted to
reported strict double rather than silently floored.}

\item{feature_weight, structure_weight}{Explicit nonnegative feature and
structure coefficients. Supply both instead of \code{alpha}. Unlike FGW share
aliases, FUGW coefficients are not normalized because their scale is
meaningful relative to the KL penalties.}
}
\value{
A list with \code{pi_samp}, \code{pi_feat}, \code{fugw_cost}, \code{linear_cost},
\code{iterations}, \code{error}, \code{status}, and \code{converged}.
}
\description{
POT-style BCD on two couplings with KL-unbalanced OT subproblems.
}
\examples{
set.seed(1)
Cx <- as.matrix(dist(matrix(rnorm(8), 4, 2)))
Cy <- as.matrix(dist(matrix(rnorm(10), 5, 2)))
out <- fugw_kl(Cx, Cy, epsilon = 0.05, max_iter = 20L)
out$status
}
