% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pot_api.R
\name{gromov_wasserstein}
\alias{gromov_wasserstein}
\title{Exact Gromov-Wasserstein via Conditional Gradient (square loss)}
\usage{
gromov_wasserstein(
  C1,
  C2,
  p = NULL,
  q = NULL,
  loss_fun = "square_loss",
  symmetric = TRUE,
  G0 = NULL,
  max_iter = 500L,
  tol_rel = 1e-09,
  tol_abs = 1e-09,
  lp_solver = c("cpp_transport", "lp_transport", "lp_matrix"),
  lp_max_iter = 20000L,
  lp_tol = 1e-12
)
}
\arguments{
\item{C1}{Source structure cost matrix (\verb{ns x ns}).}

\item{C2}{Target structure cost matrix (\verb{nt x nt}).}

\item{p}{Source weights (default uniform).}

\item{q}{Target weights (default uniform).}

\item{loss_fun}{Inner loss, currently only \code{"square_loss"} is supported.}

\item{symmetric}{Assume \code{C1} and \code{C2} are symmetric if \code{TRUE}.}

\item{G0}{Optional initial coupling (\verb{ns x nt}) warm start.}

\item{max_iter}{Max outer GW iterations.}

\item{tol_rel}{Relative stopping tolerance for exact CG.}

\item{tol_abs}{Absolute stopping tolerance for exact CG.}

\item{lp_solver}{LP backend (\code{"cpp_transport"}, \code{"lp_transport"}, \code{"lp_matrix"}).}

\item{lp_max_iter}{Maximum iterations for C++ transport simplex LP backend.}

\item{lp_tol}{Optimality tolerance for C++ transport simplex LP backend.}
}
\value{
A list with \code{plan}, \code{gw_dist}, \code{iterations}, \code{error}, \code{loss_trace}.
}
\description{
POT-compatible exact GW wrapper using the optimized exact FGW core with
zero feature cost and \code{alpha = 1}.
}
