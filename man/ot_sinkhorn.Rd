% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linear_ot.R
\name{ot_sinkhorn}
\alias{ot_sinkhorn}
\title{Balanced entropic optimal transport}
\usage{
ot_sinkhorn(
  M,
  p = NULL,
  q = NULL,
  epsilon = 0.05,
  method = c("scaling", "log", "auto"),
  max_iter = 1000L,
  tol = 1e-09
)
}
\arguments{
\item{M}{Cost matrix (\verb{ns x nt}).}

\item{p}{Source weights (default uniform). Renormalized to sum 1.}

\item{q}{Target weights (default uniform). Renormalized to sum 1.}

\item{epsilon}{Positive entropic regularization.}

\item{method}{\code{"scaling"}, \code{"log"}, or \code{"auto"}. Auto uses scaling only
when the maximum scaled exponent magnitude and span are at most 500;
otherwise it selects the genuine log-domain implementation.}

\item{max_iter}{Maximum Sinkhorn iterations.}

\item{tol}{Marginal residual tolerance.}
}
\value{
An \code{rfugw_result} with \code{plan}, \code{ot_dist}, \code{status}, and residuals.
}
\description{
Scaling or log-domain Sinkhorn for a linear cost. Reported \code{ot_dist} is
the unregularized \verb{<M, plan>} cost.
}
\examples{
M <- matrix(c(0, 1, 1, 0), 2, 2)
out <- ot_sinkhorn(M, epsilon = 0.1)
out$status
rfugw_value(out)
}
