% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pot_api.R
\name{entropic_semirelaxed_fused_gromov_wasserstein}
\alias{entropic_semirelaxed_fused_gromov_wasserstein}
\title{Entropic Semirelaxed Fused Gromov-Wasserstein (square loss)}
\usage{
entropic_semirelaxed_fused_gromov_wasserstein(
  M,
  C1,
  C2,
  p = NULL,
  loss_fun = "square_loss",
  symmetric = NULL,
  epsilon = 0.1,
  alpha = 0.5,
  G0 = NULL,
  max_iter = 10000L,
  tol = 1e-09,
  check_every = 10L,
  verbose = FALSE,
  precision = c("mixed", "double"),
  backend = c("cpp", "r")
)
}
\arguments{
\item{M}{Cross-domain feature cost matrix (\verb{ns x nt}).}

\item{C1}{Source structure cost matrix (\verb{ns x ns}).}

\item{C2}{Target structure cost matrix (\verb{nt x nt}).}

\item{p}{Source weights (default uniform).}

\item{loss_fun}{Inner loss, currently only \code{"square_loss"} is supported.}

\item{symmetric}{If \code{NULL}, inferred from \code{C1} and \code{C2}; otherwise logical.}

\item{epsilon}{Entropic regularization.}

\item{alpha}{Feature/structure trade-off in \verb{[0, 1]}.}

\item{G0}{Optional initial semirelaxed plan (\verb{ns x nt}) with row marginals \code{p}.}

\item{max_iter}{Max mirror-descent iterations.}

\item{tol}{Stopping tolerance on Frobenius norm of plan updates.}

\item{check_every}{Evaluate stopping criterion every \code{check_every} iterations.}

\item{verbose}{If \code{TRUE}, print iterative error diagnostics.}
}
\value{
A list with \code{plan}, \code{q}, \code{srfgw_dist}, \code{lin_loss}, \code{quad_loss},
\code{iterations}, and \code{error}.
}
\description{
Mirror-descent solver in KL geometry with fixed source marginal and free
target marginal.
}
