% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pot_api.R
\name{entropic_gromov_wasserstein}
\alias{entropic_gromov_wasserstein}
\title{Entropic Gromov-Wasserstein (square loss)}
\usage{
entropic_gromov_wasserstein(
  C1,
  C2,
  p = NULL,
  q = NULL,
  loss_fun = "square_loss",
  epsilon = 0.1,
  symmetric = NULL,
  G0 = NULL,
  max_iter = 1000L,
  tol = 1e-09,
  solver = c("PGD", "PPA"),
  sinkhorn_max_iter = 1000L,
  sinkhorn_tol = 1e-09,
  sinkhorn_method = c("auto", "scaling", "log"),
  precision = c("mixed", "double", "strict_double"),
  check_every = 1L,
  structure_rank = 0L
)
}
\arguments{
\item{C1}{Source structure cost matrix (\verb{ns x ns}).}

\item{C2}{Target structure cost matrix (\verb{nt x nt}).}

\item{p}{Source weights (default uniform).}

\item{q}{Target weights (default uniform).}

\item{loss_fun}{Inner loss, currently only \code{"square_loss"} is supported.}

\item{epsilon}{Entropic regularization.}

\item{symmetric}{Assume \code{C1} and \code{C2} are symmetric if \code{TRUE}.}

\item{G0}{Optional initial coupling (\verb{ns x nt}) warm start.}

\item{max_iter}{Max outer GW iterations.}

\item{tol}{Outer stopping tolerance on plan updates.}

\item{solver}{Either \code{"PGD"} or \code{"PPA"}.}

\item{sinkhorn_max_iter}{Max Sinkhorn iterations per outer step.}

\item{sinkhorn_tol}{Sinkhorn stopping tolerance.}

\item{sinkhorn_method}{Sinkhorn variant (\code{"scaling"} or \code{"log"}).}

\item{precision}{Numeric precision mode (\code{"mixed"}, \code{"double"}, or
\code{"strict_double"}).}

\item{check_every}{Evaluate stopping criterion every \code{check_every} iterations.}

\item{structure_rank}{Optional low-rank rank for structure tensor product.}
}
\value{
A list with \code{plan}, \code{gw_dist}, \code{iterations}, \code{error}.
}
\description{
POT-compatible GW wrapper using the optimized FGW entropic core with a zero
feature cost and \code{alpha = 1}.
}
