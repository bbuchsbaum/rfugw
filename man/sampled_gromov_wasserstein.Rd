% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pot_api_ports.R
\name{sampled_gromov_wasserstein}
\alias{sampled_gromov_wasserstein}
\title{Sampled Gromov-Wasserstein (square loss)}
\usage{
sampled_gromov_wasserstein(
  C1,
  C2,
  p = NULL,
  q = NULL,
  loss_fun = "square_loss",
  nb_samples_grad = 100L,
  epsilon = 1,
  max_iter = 500L,
  log = FALSE,
  verbose = FALSE,
  random_state = NULL,
  sinkhorn_max_iter = 200L,
  sinkhorn_tol = 1e-09,
  lp_solver = c("lp_matrix", "lp_transport"),
  lp_scale = 1e+06
)
}
\arguments{
\item{C1}{Source structure matrix.}

\item{C2}{Target structure matrix.}

\item{p}{Source weights (default uniform).}

\item{q}{Target weights (default uniform).}

\item{loss_fun}{Currently only \code{"square_loss"} is supported.}

\item{nb_samples_grad}{Number of sampled gradient points, or length-2 vector
\verb{(n_source_samples, n_target_samples)}.}

\item{epsilon}{Entropic regularization for the OT projection step. If \verb{<= 0},
exact LP projection is used.}

\item{max_iter}{Maximum stochastic iterations.}

\item{log}{If \code{TRUE}, return objective estimate and diagnostics.}

\item{verbose}{If \code{TRUE}, print iterative diagnostics.}

\item{random_state}{Optional seed.}

\item{sinkhorn_max_iter}{Sinkhorn iterations when \code{epsilon > 0}.}

\item{sinkhorn_tol}{Sinkhorn tolerance when \code{epsilon > 0}.}

\item{lp_solver}{LP backend used when \code{epsilon <= 0}.}

\item{lp_scale}{Integer scaling for LP marginals.}
}
\value{
If \code{log = FALSE}, returns coupling matrix \code{T}. If \code{log = TRUE},
returns a list with \code{plan}, \code{gw_dist_estimated}, and \code{iterations}.
}
\description{
POT-style stochastic GW estimator using sampled gradients and balanced OT
projection steps.
}
