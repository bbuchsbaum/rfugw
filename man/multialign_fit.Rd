% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiset.R
\name{multialign_fit}
\alias{multialign_fit}
\title{Fit Multiset Alignment to a Fixed Template}
\usage{
multialign_fit(
  subjects,
  template_mode = c("fixed", "learned"),
  template = NULL,
  k_template = NULL,
  method = c("fgw_entropic", "fugw_kl"),
  alpha = 0.5,
  epsilon = 0.05,
  feature_normalization = c("none", "zscore"),
  feature_cost_metric = c("euclidean", "sqeuclidean"),
  structure_normalize = TRUE,
  structure_knn = 0L,
  structure_knn_min_n = 600L,
  feature_cost_normalize = TRUE,
  solver = c("PGD", "PPA"),
  precision = c("mixed", "double"),
  sinkhorn_method = c("scaling", "log"),
  max_iter = 200L,
  tol = 1e-09,
  sinkhorn_max_iter = 500L,
  sinkhorn_tol = 1e-09,
  reg_marginals = c(10, 10),
  max_iter_ot = 500L,
  tol_ot = 1e-07,
  rescale_plan = TRUE,
  check_every = 10L,
  coarse_init = c("none", "sampled_graph"),
  coarse_init_components = 10L,
  coarse_init_diffusion_time = 1,
  coarse_init_self_loop = 1e-06,
  coarse_init_nb_samples = c(8L, 2L),
  coarse_init_epsilon = 0.1,
  coarse_init_max_iter = 40L,
  coarse_init_use_cpp = TRUE,
  coarse_init_sampling = c("stochastic", "deterministic"),
  structure_rank = 0L,
  autotune = FALSE,
  autotune_level = c("moderate", "aggressive"),
  seed = 1L,
  use_cpp_batch = TRUE,
  use_cpp_feature_fused = TRUE,
  use_warm_start = TRUE,
  n_threads = 1L,
  batch_min_subjects = 2L,
  template_max_iter = 8L,
  template_tol = 1e-06,
  template_relax = 1,
  barycenter_eps = 1e-12,
  final_refit = FALSE
)
}
\arguments{
\item{subjects}{A non-empty list of subject sets. Each subject is a list with
\code{C} (required), optional \code{F}, optional \code{w}, optional \code{id}.}

\item{template_mode}{Template strategy: \code{"fixed"} (fit couplings to a fixed
template) or \code{"learned"} (alternating align-and-update barycenter-style
template refinement).}

\item{template}{Fixed template set with the same fields as a subject list.
If \code{NULL}, a template is built from pooled subject features via
\code{\link[=multialign_make_template]{multialign_make_template()}}.}

\item{k_template}{Number of template nodes used only when \code{template = NULL}.}

\item{method}{Alignment backend: \code{"fgw_entropic"} or \code{"fugw_kl"}.}

\item{alpha}{Feature/structure trade-off parameter.}

\item{epsilon}{Entropic regularization for the selected backend.}

\item{feature_normalization}{Feature normalization mode before cost assembly.}

\item{feature_cost_metric}{Feature cost metric used to form cross-set \code{M}.}

\item{structure_normalize}{If \code{TRUE}, each structure matrix is scaled to have
max value 1 before solving.}

\item{structure_knn}{Optional kNN sparsification level for structure matrices.
If positive, rows keep \code{k+1} smallest entries (including diagonal) and
other entries are set to a large fill value before solving.}

\item{structure_knn_min_n}{Minimum matrix size for applying \code{structure_knn}.}

\item{feature_cost_normalize}{If \code{TRUE}, each cross-feature cost matrix is
scaled to have max value 1.}

\item{solver}{\code{fgw_entropic} solver (\code{"PGD"} or \code{"PPA"}).}

\item{precision}{Numeric precision for \code{fgw_entropic} (defaults to \code{"mixed"}
for speed in multiset alignment).}

\item{sinkhorn_method}{Sinkhorn variant for \code{fgw_entropic}.}

\item{max_iter}{Outer iterations (\code{fgw_entropic}) or BCD iterations (\code{fugw_kl}).}

\item{tol}{Outer stopping tolerance for selected backend.}

\item{sinkhorn_max_iter}{Inner Sinkhorn iterations for \code{fgw_entropic}.}

\item{sinkhorn_tol}{Inner Sinkhorn tolerance for \code{fgw_entropic}.}

\item{reg_marginals}{Marginal relaxation for \code{fugw_kl}.}

\item{max_iter_ot}{Inner Sinkhorn iterations for \code{fugw_kl}.}

\item{tol_ot}{Inner Sinkhorn tolerance for \code{fugw_kl}.}

\item{rescale_plan}{Plan rescaling option for \code{fugw_kl}.}

\item{check_every}{Iteration check interval for selected backend.}

\item{coarse_init}{Optional coarse coupling initialization mode. \code{"none"}
disables coarse initialization. \code{"sampled_graph"} computes a fast
sampled-GW plan on graph-derived diffusion coordinates to initialize FGW.}

\item{coarse_init_components}{Diffusion components for \code{"sampled_graph"}.}

\item{coarse_init_diffusion_time}{Diffusion-time exponent for coarse init.}

\item{coarse_init_self_loop}{Diagonal stabilizer for coarse graph init.}

\item{coarse_init_nb_samples}{Sample counts for coarse sampled-GW gradients.}

\item{coarse_init_epsilon}{Entropic regularization for coarse init.}

\item{coarse_init_max_iter}{Iterations for coarse sampled-GW init.}

\item{coarse_init_use_cpp}{Use C++ fast path for coarse sampled-GW init.}

\item{coarse_init_sampling}{Sampling mode for coarse init.}

\item{structure_rank}{Optional low-rank rank used in the FGW tensor-product
update. Use \code{0}/\code{"off"} to disable or \code{"auto"} for size-aware rank
selection.}

\item{autotune}{If \code{TRUE}, apply size-aware caps to FGW iterations for faster
large-\code{n} runs.}

\item{autotune_level}{Autotune aggressiveness.}

\item{seed}{Optional seed used only when \code{template = NULL}.}

\item{use_cpp_batch}{If \code{TRUE}, use the C++ batched FGW path for fixed-template
\code{method = "fgw_entropic"}.}

\item{use_cpp_feature_fused}{If \code{TRUE} and conditions allow, use a fused C++
path that computes feature costs and solves FGW in one batched call without
materializing \code{M_list} in R.}

\item{use_warm_start}{If \code{TRUE}, reuse previous couplings as initialization
across learned-template outer iterations (and for \code{fugw_kl} fixed solves).}

\item{n_threads}{Requested subject-parallel threads for the C++ batched path.}

\item{batch_min_subjects}{Minimum number of subjects before enabling batch path.}

\item{template_max_iter}{Maximum alternating updates for \code{template_mode = "learned"}.}

\item{template_tol}{Relative stopping tolerance for learned-template updates.}

\item{template_relax}{Relaxation in \verb{(0, 1]} when updating learned templates.}

\item{barycenter_eps}{Stabilizer used in learned-template denominator updates.}

\item{final_refit}{If \code{TRUE}, run one final alignment against the last updated
learned template. If \code{FALSE} (default), skip this extra pass for speed.}
}
\value{
A list with \code{couplings}, \code{objectives}, \code{template}, \code{diagnostics},
and run settings.
}
\description{
Aligns a list of attributed metric-measure sets to a fixed template using
FGW (\code{method = "fgw_entropic"}) or unbalanced FUGW
(\code{method = "fugw_kl"}). This is the first stage of general multiset
alignment and returns subject-to-template couplings.
}
