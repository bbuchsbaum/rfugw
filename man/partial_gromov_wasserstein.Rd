% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pot_api_ports.R
\name{partial_gromov_wasserstein}
\alias{partial_gromov_wasserstein}
\title{Partial Gromov-Wasserstein (square loss)}
\usage{
partial_gromov_wasserstein(
  C1,
  C2,
  p = NULL,
  q = NULL,
  m = NULL,
  loss_fun = "square_loss",
  nb_dummies = 1L,
  G0 = NULL,
  thres = 1,
  numItermax = 10000L,
  tol = 1e-08,
  symmetric = NULL,
  warn = TRUE,
  log = FALSE,
  verbose = FALSE,
  lp_solver = c("cpp_transport", "lp_matrix", "lp_transport"),
  lp_scale = 1e+06,
  ...
)
}
\arguments{
\item{C1}{Source structure cost matrix (\verb{ns x ns}).}

\item{C2}{Target structure cost matrix (\verb{nt x nt}).}

\item{p}{Source weights (default uniform).}

\item{q}{Target weights (default uniform).}

\item{m}{Amount of mass to transport. Defaults to \code{min(sum(p), sum(q))}.}

\item{loss_fun}{Inner loss, currently only \code{"square_loss"} is supported.}

\item{nb_dummies}{Number of dummy nodes used in the partial OT linearized step.}

\item{G0}{Optional initial coupling (\verb{ns x nt}) warm start.}

\item{thres}{Unused POT compatibility parameter.}

\item{numItermax}{Maximum CG iterations.}

\item{tol}{Relative stopping tolerance on the partial objective.}

\item{symmetric}{Assume \code{C1} and \code{C2} are symmetric if \code{TRUE}.}

\item{warn}{Ignored; retained for POT signature compatibility.}

\item{log}{If \code{TRUE}, return a list with diagnostics; otherwise the plan.}

\item{verbose}{If \code{TRUE}, print CG diagnostics.}

\item{lp_solver}{LP backend for the linearized partial OT step
(\code{"cpp_transport"} default, \code{"lp_transport"}, \code{"lp_matrix"}).}

\item{lp_scale}{Integer scaling factor for LP marginal discretization.}

\item{...}{Additional arguments. Unused extras are rejected when the solver uses \code{.reject_unused_dots()}; otherwise they are forwarded to the primary solver.}
}
\value{
If \code{log = FALSE}, returns a coupling matrix. If \code{log = TRUE}, returns
a list with \code{plan}, \code{partial_gw_dist}, \code{iterations}, \code{error}, and \code{loss_trace}.
}
\description{
POT-compatible exact partial GW solver using conditional gradient with
partial-OT direction steps.
}
