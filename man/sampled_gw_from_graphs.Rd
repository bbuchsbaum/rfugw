% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparse_graph_ot.R
\name{sampled_gw_from_graphs}
\alias{sampled_gw_from_graphs}
\title{Sampled GW from Sparse or Dense Similarity Graphs}
\usage{
sampled_gw_from_graphs(
  W1,
  W2,
  n_components = 16L,
  diffusion_time = 1,
  symmetrize = TRUE,
  self_loop = 1e-06,
  tol = 1e-12,
  p = NULL,
  q = NULL,
  metric = c("euclidean", "sqeuclidean"),
  loss_fun = "square_loss",
  nb_samples_grad = 100L,
  epsilon = 1,
  max_iter = 500L,
  log = FALSE,
  verbose = FALSE,
  random_state = NULL,
  sinkhorn_max_iter = 200L,
  sinkhorn_tol = 1e-09,
  lp_solver = c("lp_matrix", "lp_transport"),
  lp_scale = 1e+06,
  use_cpp = TRUE,
  objective_samples = 2000L,
  return_embeddings = FALSE,
  sampling = c("stochastic", "deterministic")
)
}
\arguments{
\item{W1}{Source similarity graph (\verb{n1 x n1}) as dense matrix or sparse
\code{Matrix::sparseMatrix}.}

\item{W2}{Target similarity graph (\verb{n2 x n2}) as dense matrix or sparse
\code{Matrix::sparseMatrix}.}

\item{n_components}{Number of diffusion coordinates.}

\item{diffusion_time}{Diffusion-time exponent.}

\item{symmetrize}{If \code{TRUE}, symmetrize each graph.}

\item{self_loop}{Optional diagonal regularization added before diffusion map.}

\item{tol}{Degree floor for graph validity checks.}

\item{p}{Source weights (default uniform).}

\item{q}{Target weights (default uniform).}

\item{metric}{Ground metric for coordinate GW.}

\item{loss_fun}{Currently only \code{"square_loss"} is supported.}

\item{nb_samples_grad}{Number of sampled gradient points, or length-2 vector
\verb{(n_source_samples, n_target_samples)}. Values below 1 error. Source or
target counts above \code{ns} / \code{nt} warn and clamp.}

\item{epsilon}{Entropic regularization.}

\item{max_iter}{Maximum stochastic iterations.}

\item{log}{If \code{TRUE}, return diagnostics.}

\item{verbose}{If \code{TRUE}, print iterative diagnostics.}

\item{random_state}{Optional seed.}

\item{sinkhorn_max_iter}{Sinkhorn iterations when \code{epsilon > 0}.}

\item{sinkhorn_tol}{Sinkhorn tolerance when \code{epsilon > 0}.}

\item{lp_solver}{LP backend when \code{epsilon <= 0}.}

\item{lp_scale}{Integer scaling for LP marginals.}

\item{use_cpp}{Use C++ fast path for entropic coordinate solver when
available.}

\item{objective_samples}{Monte Carlo samples for objective estimation.}

\item{return_embeddings}{If \code{TRUE}, include diffusion embeddings in output.}

\item{sampling}{Sampling policy passed to
\code{\link[=sampled_gromov_wasserstein_coords]{sampled_gromov_wasserstein_coords()}}.}
}
\value{
Coupling matrix or diagnostics list, mirroring
\code{sampled_gromov_wasserstein_coords}.
}
\description{
Convenience wrapper that converts similarity graphs into diffusion
coordinates and solves sampled GW in coordinate space.
}
\section{Experimental}{

Graph-then-sampled GW is experimental. The certified 0.1 envelope is
the same quality-versus-budget claim as
\code{\link[=sampled_gromov_wasserstein_coords]{sampled_gromov_wasserstein_coords()}}, plus that a sparse similarity
graph plus \code{k} diffusion coordinates stores less than two dense
\verb{n x n} structure costs. See \code{inst/bench/sampled-budget-curves.md}.
}

