% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ot_utils.R
\name{ot_validate_plan}
\alias{ot_validate_plan}
\title{Validate a transport plan}
\usage{
ot_validate_plan(
  plan,
  p = NULL,
  q = NULL,
  mass = NULL,
  marginals = c("balanced", "partial", "relaxed"),
  tol = 1e-08
)
}
\arguments{
\item{plan}{Coupling matrix or result object.}

\item{p}{Optional source weights.}

\item{q}{Optional target weights.}

\item{mass}{Optional required total mass.}

\item{marginals}{\code{"balanced"} (row/col match \code{p}/\code{q}), \code{"partial"}
(row/col do not exceed \code{p}/\code{q} and total mass matches), or \code{"relaxed"}
(nonnegativity and finiteness only).}

\item{tol}{Residual tolerance.}
}
\value{
The plan, invisibly, after validation.
}
\description{
Checks shape, nonnegativity, total mass, and optional marginal constraints.
Accepts a raw matrix or an \code{rfugw_result}.
}
\examples{
M <- matrix(c(0, 1, 1, 0), 2, 2)
out <- ot_emd(M)
ot_validate_plan(out, c(0.5, 0.5), c(0.5, 0.5))
ot_linear_cost(M, out)
ot_entropy(out)
}
