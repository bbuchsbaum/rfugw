% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linear_ot.R
\name{ot_partial_emd}
\alias{ot_partial_emd}
\title{Exact partial linear optimal transport}
\usage{
ot_partial_emd(M, p = NULL, q = NULL, mass = 1, max_iter = 20000L, tol = 1e-12)
}
\arguments{
\item{M}{Cost matrix (\verb{ns x nt}).}

\item{p}{Source weights (default uniform). Renormalized to sum 1.}

\item{q}{Target weights (default uniform). Renormalized to sum 1.}

\item{mass}{Transported probability mass in \verb{[0, 1]}.}

\item{max_iter}{Maximum simplex iterations.}

\item{tol}{Optimality tolerance.}
}
\value{
An \code{rfugw_result} with \code{plan}, \code{ot_dist}, \code{partial_ot_dist}, partial
feasibility and mass certificates, and the exact certificate for the
equivalent augmented transport problem. Augmented dual potentials include
the final dummy coordinate.
}
\description{
Solves the nonnegative-cost partial transport problem
\verb{min <M, G>} subject to \code{rowSums(G) <= p}, \code{colSums(G) <= q}, and
\code{sum(G) = mass}. Weights are normalized to probability vectors, so \code{mass}
is in \verb{[0, 1]}. The implementation reduces the problem to the certified
exact transport primitive with one dummy source and target; it does not add
a second simplex implementation.
}
\examples{
M <- matrix(c(0, 3, 2, 0), 2, 2)
out <- ot_partial_emd(M, mass = 0.5)
out$status
sum(rfugw_plan(out))
}
