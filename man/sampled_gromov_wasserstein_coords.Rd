% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparse_graph_ot.R
\name{sampled_gromov_wasserstein_coords}
\alias{sampled_gromov_wasserstein_coords}
\title{Sampled Gromov-Wasserstein Directly from Coordinates}
\usage{
sampled_gromov_wasserstein_coords(
  X1,
  X2,
  p = NULL,
  q = NULL,
  metric = c("euclidean", "sqeuclidean"),
  loss_fun = "square_loss",
  nb_samples_grad = 100L,
  epsilon = 1,
  max_iter = 500L,
  log = FALSE,
  verbose = FALSE,
  random_state = NULL,
  sinkhorn_max_iter = 200L,
  sinkhorn_tol = 1e-09,
  lp_solver = c("lp_matrix", "lp_transport"),
  lp_scale = 1e+06,
  use_cpp = TRUE,
  objective_samples = 2000L,
  sampling = c("stochastic", "deterministic")
)
}
\arguments{
\item{X1}{Source coordinates (\verb{ns x d}).}

\item{X2}{Target coordinates (\verb{nt x d}).}

\item{p}{Source weights (default uniform).}

\item{q}{Target weights (default uniform).}

\item{metric}{Ground metric between coordinates (\code{"euclidean"} or
\code{"sqeuclidean"}).}

\item{loss_fun}{Currently only \code{"square_loss"} is supported.}

\item{nb_samples_grad}{Number of sampled gradient points, or length-2 vector
\verb{(n_source_samples, n_target_samples)}. Values below 1 error. Source or
target counts above \code{ns} / \code{nt} warn and clamp.}

\item{epsilon}{Entropic regularization for projection step.}

\item{max_iter}{Maximum stochastic iterations.}

\item{log}{If \code{TRUE}, return diagnostics.}

\item{verbose}{If \code{TRUE}, print iterative diagnostics.}

\item{random_state}{Optional seed.}

\item{sinkhorn_max_iter}{Sinkhorn iterations when \code{epsilon > 0}.}

\item{sinkhorn_tol}{Sinkhorn tolerance when \code{epsilon > 0}.}

\item{lp_solver}{LP backend when \code{epsilon <= 0}.}

\item{lp_scale}{Integer scaling for LP marginals.}

\item{use_cpp}{If \code{TRUE} and C++ kernel is available, use the optimized
entropic coordinate-native kernel when \code{epsilon > 0}.}

\item{objective_samples}{Number of Monte Carlo samples for
\code{gw_dist_estimated} when \code{log = TRUE}.}

\item{sampling}{Sampling policy for stochastic updates. \code{"stochastic"} uses
weighted random sampling (POT-style). \code{"deterministic"} uses top-k
probability selections for reproducible parity checks.}
}
\value{
If \code{log = FALSE}, returns coupling matrix \code{T}. If \code{log = TRUE},
returns a list with \code{plan}, \code{gw_dist_estimated}, and \code{iterations}.
}
\description{
POT-style sampled GW solver that operates on point coordinates and computes
required structure-distance rows on the fly. This avoids storing dense
\verb{n x n} distance matrices and is suitable after sparse-graph diffusion
embeddings.
}
\section{Experimental}{

Coordinate-native sampled GW is experimental. The certified 0.1
envelope matches \code{\link[=sampled_gromov_wasserstein]{sampled_gromov_wasserstein()}}: a full budget
\verb{(ns, nt)} is closer to dense entropic GW than a tiny budget.
Intermediate budgets are not certified as monotone. Inputs scale as
\verb{O(n d)} rather than \code{O(n^2)} structure costs. See
\code{inst/bench/sampled-budget-curves.md}.
}

